<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp" />
    <solid android:color="@color/darkgray" /> <!-- Cor de fundo do pop-up -->
    <stroke
        android:width="1dp"
        android:color="@color/azul_claro" /> <!-- Cor da borda do pop-up -->
</shape>
